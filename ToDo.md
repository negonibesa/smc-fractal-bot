ФИНАЛЬНАЯ АРХИТЕКТУРА (для агента)
text
smc_fond_bot/
│
├── config/
│   └── settings.yaml         # Все настройки (через YAML, не через .py)
│
├── core/
│   ├── data_loader.py        # Загрузка с Bybit + кеш
│   ├── strategy.py           # SMC-логика (свип → центр → вход)
│   ├── risk_manager.py       # Риск 1%, расчет объема, лимиты
│   ├── order_executor.py     # API-клиент (Bybit)
│   ├── position_tracker.py   # Открытые позиции
│   └── state_manager.py      # Redis (состояние, сделки, PnL)
│
├── optimize/
│   ├── wfo_runner.py         # Walk-Forward оптимизация
│   ├── param_storage.py      # Хранение лучших параметров
│   └── triggers.py           # Условия для автооптимизации
│
├── notifiers/
│   ├── telegram.py           # Уведомления (вход, выход, ошибки)
│   └── alerts.py             # Критические алерты (просадка, лимиты)
│
├── maintenance/
│   ├── log_rotator.py        # Авточистка логов (старше 30 дней)
│   ├── data_cleaner.py       # Удаление старых данных (>90 дней)
│   └── health_check.py       # Проверка API, Redis, диска, памяти
│
├── main.py                   # Точка входа
├── deploy.sh                 # Деплой на VPS
├── requirements.txt
└── .env                      # Ключи (не в репозитории)
📋 TODO ДЛЯ АГЕНТА (lfq)
Скопируй этот список и скинь ему как задачу.

markdown
# TODO: МИНИ-ФОНД — АВТОНОМНАЯ СИСТЕМА

## 1. БАЗА
[ ] Переделать все настройки в `config/settings.yaml`
[ ] Убрать `config.py`, использовать YAML + переменные окружения
[ ] Добавить `USE_REDIS = True` в настройки

## 2. REDIS — СОСТОЯНИЕ
[ ] Установить redis-py
[ ] `state_manager.py`:
    [ ] save_trade(symbol, trade_data)
    [ ] get_trades_today(symbol)
    [ ] get_daily_pnl(symbol)
    [ ] save_balance(symbol, balance)
    [ ] get_balance(symbol)
    [ ] save_last_run(symbol) / get_last_run(symbol)
[ ] При запуске проверять через Redis, не было ли уже сделок сегодня
[ ] Если сделка была, не открывать новую (MAX_POSITIONS = 1)

## 3. АВТООПТИМИЗАЦИЯ (условия)
[ ] Запускать WFO при ОДНОМ из условий:
    [ ] Прошло 30 дней с последней оптимизации
    [ ] Сделано > 20 сделок после последней оптимизации
    [ ] PF упал ниже 1.2 на последних 20 сделках
[ ] Если новые параметры > старых на 10% (PF) → заменить
[ ] Сохранять историю параметров в Redis (последние 3 версии)

## 4. АВТОЧИСТКА (чтобы не упереться в лимиты хостинга)
[ ] log_rotator.py:
    [ ] Удалять логи старше 30 дней
    [ ] Сжимать логи старше 7 дней (gzip)
[ ] data_cleaner.py:
    [ ] Удалять сырые данные старше 90 дней
    [ ] Оставлять только последние 5000 свечей 1H
[ ] health_check.py:
    [ ] Проверка свободного места на диске (< 1GB → alert)
    [ ] Проверка памяти (< 256MB → alert)
    [ ] Проверка Redis (если не отвечает → перезапуск)

## 5. ЛИМИТЫ И БЕЗОПАСНОСТЬ
[ ] Остановка бота при просадке > 10% от пика (Redis хранит пик)
[ ] Остановка бота при дневном убытке > 5%
[ ] Остановка бота при 3 убыточных сделках подряд
[ ] Если бот остановлен → Telegram + запись в лог
[ ] Автоматический перезапуск бота раз в сутки (чтобы сбросить память)

## 6. ТЕЛЕГРАМ-ОПОВЕЩЕНИЯ
[ ] Вход: символ, цена, стоп, тейк
[ ] Выход: причина (тейк/стоп/трейлинг), PnL
[ ] Ошибки: API ошибки, падение Redis, лимиты
[ ] Ежедневный отчет: сделки за день, PnL, баланс

## 7. ЗАПУСК
[ ] main.py:
    [ ] Проверка Redis
    [ ] Проверка API-ключей
    [ ] Проверка свободного места
    [ ] Запуск основного цикла
    [ ] Обработка Ctrl+C (грамотное завершение)

## 8. ДЕПЛОЙ
[ ] deploy.sh:
    [ ] git pull
    [ ] pip install -r requirements.txt
    [ ] redis-server (проверка)
    [ ] screen -S bot python main.py
    [ ] Настройка cron для ежемесячной оптимизации